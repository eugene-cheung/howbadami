# Optional: URL of the analysis server (defaults to your computer at 8000 for local dev).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production (Vercel / etc.): set to your public API origin, no trailing slash, e.g.
# NEXT_PUBLIC_API_URL=https://howbadami-api.onrender.com
